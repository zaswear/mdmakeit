# Blueprint de Render — servicio web Docker
# Importa este repo en Render (New → Blueprint) y desplegará con esta config.
services:
  - type: web
    name: mdmakeit
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: PYTHON_PATH
        value: /opt/venv/bin/python
